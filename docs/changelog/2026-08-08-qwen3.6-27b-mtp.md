## 2026-08-08 - Add Qwen3.6 27B MTP

**What**: Add the Q4_K_M and IQ4_XS quantizations of
`Qwen3.6-27B-MTP-GGUF`, with full-context `agentic` profiles, 131,072-token
`text` profiles, and IQ4_XS alternatives.

**Where**: The manifest is in `config/models/qwen3.6-27b-mtp.psd1`, the
launchers are in `scripts/models/qwen3.6-27b-mtp/`, and model-specific
validation is in `scripts/common/Test-Llm.ps1`.

**Why**: The Windows environment needs a reproducible hybrid-offload MTP
deployment without installing `UD-Q4_K_XL` or a multimodal projector.

## 2026-08-08 - Finalize fit and MTP profiles

**What**: Keep `--fit on --fit-target 768`, retain the 262,144-token and
131,072-token context windows, and select `--spec-draft-n-max 2` after testing
values 1 through 4.

**Where**: The four self-contained launchers and `scripts/common/Test-Llm.ps1`,
which supports temporary `-SpecDraftNMax` overrides without editing launchers.

**Why**: Automatic fitting left approximately 983 MiB free on the RTX 5080 in
native Windows WDDM measurements while avoiding a fixed GPU-layer count.
