# 2026-08-16 - Sync Nemotron 3.5 agentic profile

## What

Synchronize the Nemotron launcher with the current Windows source tree. The
active profile now uses a 131,072-token context, `n-cpu-moe 23`, and
`--batch-size 2048 --ubatch-size 2048`. Remove the obsolete text launcher,
which no longer exists in the Windows model directory.

## Where

- `scripts/models/nemotron-3.5-lightning-30b-a3b/`
- `scripts/common/Test-Llm.ps1`
- `README.md`
- `docs/overview/model-management.md`
- `docs/overview/models/nemotron-3.5-lightning-30b-a3b.md`

## Why

Keep `local-inference-setup` aligned with the current reproducible Nemotron
configuration instead of retaining a stale text profile with different
context, CPU-MoE offload, and batching values.

## Validation

- Compared the launcher with `D:\LLM\scripts\models\nemotron-3.5-lightning-30b-a3b\start-agentic.cmd`.
- Confirmed that the repository launcher inventory is expected to contain 17
  profiles after removing the obsolete text launcher.
