## 2026-08-16 - Finalize Qwen3.6 agentic profiles

**What**: Add the validated `agentic-131k-4096` and
`agentic-mtp-131k-4096` profiles, and align the existing `agentic` and
`agentic-mtp` profiles with their final `1024/1024` batch settings.

**Where**: The four self-contained launchers in
`scripts/models/qwen3.6-35b-a3b/`, the model card, `README.md`,
`docs/overview/model-management.md`, and the launcher-count assertion in
`scripts/common/Test-Llm.ps1`.

**Why**: Preserve two reproducible operating points for agentic coding: the
full-context variants with 262,144 tokens and `1024/1024`, and the validated
131,072-token high-prefill variants with `4096/4096` without MTP or
`2048/2048` with MTP. The repository continues to pin its existing
`b10273-cuda13.3` runtime.
